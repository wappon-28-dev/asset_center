{
  "name": "assets_center",
  "description": "assets_center",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cd packages/client && bun run build",
    "dev": "cd packages/client && bun run dev",
    "dev:api": "cd packages/api && bun run dev",
    "prepare": "cd packages/client && bun run prepare"
  },
  "workspaces": [
    "packages/*"
  ]
}
